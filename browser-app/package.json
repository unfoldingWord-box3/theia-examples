{
  "private": true,
  "name": "browser-app",
  "version": "0.0.0",
  "dependencies": {
    "@theia/core": "1.57.1",
    "@theia/preferences": "1.57.1",
    "backendService": "0.0.0",
    "empty": "0.0.0",
    "widgetExtension": "0.0.0"
  },
  "devDependencies": {
    "@theia/cli": "1.57.1"
  },
  "scripts": {
    "bundle": "yarn rebuild && theia build --mode development",
    "rebuild": "theia rebuild:browser --cacheRoot ..",
    "start": "theia start",
    "watch": "yarn rebuild && theia build --watch --mode development"
  },
  "theia": {
    "target": "browser"
  }
}
