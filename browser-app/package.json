{
  "private": true,
  "name": "checking-browser-app",
  "version": "0.0.0",
  "dependencies": {
    "@theia/core": "1.58.2",
    "@theia/plugin-ext-vscode": "1.58.2",
    "@theia/preferences": "1.58.2",
    "empty": "0.0.0"
  },
  "devDependencies": {
    "@theia/cli": "1.58.2"
  },
  "scripts": {
    "bundle": "yarn rebuild && theia build --mode development",
    "rebuild": "theia rebuild:browser --cacheRoot ..",
    "start": "theia start --plugins=local-dir:../electron-app/plugins",
    "watch": "yarn rebuild && theia build --watch --mode development"
  },
  "theia": {
    "target": "browser"
  }
}
